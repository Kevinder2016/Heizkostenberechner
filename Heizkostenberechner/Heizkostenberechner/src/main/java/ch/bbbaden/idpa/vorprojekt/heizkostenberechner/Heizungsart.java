package ch.bbbaden.idpa.vorprojekt.heizkostenberechner;

public enum Heizungsart {
    // Variable, nur einer von denen kann gespeichert werden. Nur eine vo dene chan es si.
    ÖL,
    GAS,
    PELLETS,
    WÄRMEPUMPE
}
